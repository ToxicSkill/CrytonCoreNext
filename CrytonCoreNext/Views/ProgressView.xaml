<UserControl x:Class="CrytonCoreNext.Views.ProgressView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:busyIndicator="https://github.com/Peoky/BusyIndicator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:Width="250"
    d:Height="250">

    <Grid Resources="{StaticResource ResourceKey=indicator}" Visibility="{Binding ProgressService.ProgressVisibility}"
                            Background="Transparent">
        <Grid Visibility="{Binding ProgressService.ShowLabels, FallbackValue=Visible}">
            <Label Content="{Binding ProgressService.ProgressCounter}" Style="{StaticResource CryptingSettings}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, -120, 0 ,0"
                   FontSize="18" Foreground="LightGray" HorizontalContentAlignment="Center" VerticalContentAlignment="Top"/>
            <Label Content="{Binding ProgressService.ProgressMessage}" Style="{StaticResource CryptingSettings}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, -70, 0 ,0"
                   FontSize="22" Foreground="White" HorizontalContentAlignment="Center" VerticalContentAlignment="Top"/>
        </Grid>
    </Grid>
</UserControl>
