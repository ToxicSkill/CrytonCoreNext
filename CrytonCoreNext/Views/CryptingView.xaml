<ui:UiPage
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:models="clr-namespace:CrytonCoreNext.Crypting.Models"
    x:Class="CrytonCoreNext.Views.CryptingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="clr-namespace:CrytonCoreNext.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:CryptingViewModel}"
    xmlns:c="clr-namespace:CrytonCoreNext.Converters"
    MinWidth="900"
    d:DesignHeight="850"
    d:DesignWidth="1400"
    mc:Ignorable="d">

    <ui:UiPage.Resources>
        <ResourceDictionary>
            <c:CollectionToVisibilityConverter x:Key="CollectionToVisibilityConverter"/>
            <c:ReverseCollectionToVisibilityConverter x:Key="ReverseCollectionToVisibilityConverter"/>
            <c:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <SolidColorBrush x:Key="BrushBlueTransparent" Color="#7F8484EF"/>
        </ResourceDictionary>
    </ui:UiPage.Resources>

    <Grid>
        <Grid Margin="10, 0 ,0 ,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Crypting" FontSize="32" FontWeight="Light" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                        <ui:Button Icon="Add20" Command="{Binding LoadCryptFilesCommand}" Content="Add" Padding="6" Margin="0, 10, 10, 10" Appearance="Secondary" HorizontalAlignment="Stretch" />
                        <ui:Button Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Icon="Save20" Command="{Binding SaveCryptFileCommand}" Content="Save" Padding="6" Margin="0, 10, 10, 10" Appearance="Secondary" HorizontalAlignment="Right" />
                        <ui:Button Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Icon="Delete20" Command="{Binding DeleteFileCommand}" Content="Delete" Padding="6" Margin="0, 10, 10, 10" Appearance="Secondary" HorizontalAlignment="Right" />
                    </StackPanel>
                    <ComboBox Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="0, 10, 0, 10" Padding="6" DisplayMemberPath="DescriptionName" ItemsSource="{Binding CryptingMethods, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCryptingMethod}"/>
                </Grid>
                <Grid Grid.Row="2" Visibility="{Binding Files, Converter={StaticResource CollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged }">
                    <Border  CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BrushBlueTransparent}" Margin="0, 0, 0, -30">
                        <!--<Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#002F3BEF" Offset="1"/>
                                <GradientStop Color="#7F4CB1FF" Offset="0"/>
                            </LinearGradientBrush>
                        </Border.Background>-->
                    </Border>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="DocumentBulletListMultiple20" FontSize="62" Filled="True" FontWeight="Thin"/>
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" Margin="10, 0 ,0 ,20" Text="Here will be the files that you upload" FontWeight="Thin"/>
                        <DockPanel  HorizontalAlignment="Stretch">
                            <ui:SymbolIcon Symbol="Add20" FontSize="18" Margin="20, 0, 0 ,0"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="16" Margin="0, 0 ,40 ,0" Text="upload files" FontWeight="Light"/>
                        </DockPanel>
                        <DockPanel HorizontalAlignment="Stretch">
                            <ui:SymbolIcon Symbol="Save20" FontSize="18" Margin="20, 0, 0 ,0"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="16" Margin="0, 0 ,40 ,0" Text="save selected file" FontWeight="Light"/>
                        </DockPanel>
                        <DockPanel HorizontalAlignment="Stretch">
                            <ui:SymbolIcon Symbol="Delete20" FontSize="18" Margin="20, 0, 0 ,0"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="16" Margin="0, 0 ,40 ,0" Text="delete selected file" FontWeight="Light"/>
                        </DockPanel>
                        <DockPanel HorizontalAlignment="Stretch">
                            <ui:SymbolIcon Symbol="TextIndentIncreaseLtr20" FontSize="18" Margin="20, 0, 0 ,0"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="16" Margin="0, 0 ,40 ,0" Text="select crypting method" FontWeight="Light"/>
                        </DockPanel>
                    </StackPanel>
                </Grid>
                <ScrollViewer Grid.Row="2" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
                    <ListView ItemsSource="{Binding Files, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedFile}">
                        <ListView.Template>
                            <ControlTemplate TargetType="ListBox">
                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderBrush}">
                                    <ItemsPresenter/>
                                </Border>
                            </ControlTemplate>
                        </ListView.Template>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="models:CryptFile">
                                <Grid Height="40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="20"/>
                                        <RowDefinition Height="20"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left"
                                                FontSize="13"
                                                FontWeight="Medium"
                                                TextWrapping="WrapWithOverflow"
                                                Text="{Binding NameWithExtension}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left"
                                                FontSize="12"
                                                Text="{Binding Date}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right"
                                                FontSize="12"
                                                Text="{Binding Size}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right"
                                                FontSize="12"
                                                Text="{Binding Status,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>

            <Grid Grid.Column="1" Margin="20, 0, 0 ,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="90"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="90"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="1" Visibility="{Binding Files, Converter={StaticResource CollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                        <Border  CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BrushBlueTransparent}" >
                            <!--<Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#002F3BEF" Offset="1"/>
                                <GradientStop Color="#7F4CB1FF" Offset="0"/>
                            </LinearGradientBrush>
                        </Border.Background>-->
                        </Border>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="AppsListDetail20" FontSize="28"/>
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Here will be the details of chosen the file" FontSize="14" FontWeight="Light"/>
                        </StackPanel>
                    </Grid>
                    <Grid Grid.Column="0" Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!--<ui:Button Grid.Column="1" Grid.Row="1" Margin="10" Padding="6" HorizontalAlignment="Stretch" Icon="LockClosed20" Content="Encrypt" Appearance="Secondary"/>-->

                        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Text="Current file" FontSize="16" FontWeight="Thin"/>
                        <TextBlock Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Text="Status" FontSize="16" FontWeight="Thin"/>
                        <TextBlock Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Left" Text="Extension" FontSize="16" FontWeight="Thin"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Text="{Binding SelectedFile.Name}" HorizontalAlignment="Right" FontSize="14" FontWeight="Medium"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Text="{Binding SelectedFile.Status}" HorizontalAlignment="Right" FontSize="14" FontWeight="Medium"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" Text="{Binding SelectedFile.Extension}" HorizontalAlignment="Right" FontSize="14" FontWeight="Medium"/>
                    </Grid>

                    <Grid Grid.Column="1" Visibility="{Binding Files, Converter={StaticResource CollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Margin="20, 0, 0, 0">
                        <Border  CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BrushBlueTransparent}" Margin="0, 0, 20, 0">
                            <!--<Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#002F3BEF" Offset="1"/>
                                <GradientStop Color="#7F4CB1FF" Offset="0"/>
                            </LinearGradientBrush>
                        </Border.Background>-->
                        </Border>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, 0, 20, 0">
                            <ui:SymbolIcon Symbol="TextBaseline20" FontSize="28"/>
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Here will be name of chosen method" FontSize="14" FontWeight="Light"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Grid.Column="1" Text="{Binding CurrentCryptingViewModel.ViewModel.PageName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  
                               Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                               FontSize="78" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
                <Grid Grid.Row="2" Visibility="{Binding Files, Converter={StaticResource CollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <Border  CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BrushBlueTransparent}" Margin="0, 20, 20, 20">
                        <!--<Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#002F3BEF" Offset="1"/>
                                <GradientStop Color="#7F4CB1FF" Offset="0"/>
                            </LinearGradientBrush>
                        </Border.Background>-->
                    </Border>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="Fingerprint20" FontSize="62"/>
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Here will be the chosen crypting method settings view" FontSize="16" FontWeight="Light"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="2" Margin="0, 20, 20, 0" Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <Frame Grid.Row="1" Content="{Binding CurrentCryptingViewModel}"/>
                </Grid>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="3" Margin="18, 0, 0, 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="55"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="2" Visibility="{Binding Files, Converter={StaticResource CollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Margin="20, 0, 0, 0">
                    <Border  CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BrushBlueTransparent}" Margin="-18, 10, 20, 10">
                        <!--<Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#002F3BEF" Offset="1"/>
                                <GradientStop Color="#7F4CB1FF" Offset="0"/>
                            </LinearGradientBrush>
                        </Border.Background>-->
                    </Border>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="Fire20" FontSize="34"/>
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Here will be action panel" FontSize="14" FontWeight="Light"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="2" Visibility="{Binding Files, Converter={StaticResource ReverseCollectionToVisibilityConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <ui:CardAction Grid.Column="2" Icon="Fingerprint20" HorizontalAlignment="Right" Command="{Binding PerformCryptingCommand}">
                        <StackPanel>
                            <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="Crypting" />
                            <TextBlock
                            FontSize="12" FontWeight="UltraLight"
                            Text="Start crypting process for current file" />
                        </StackPanel>
                    </ui:CardAction>
                    <ui:ProgressRing Grid.Column="1" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" IsIndeterminate="{Binding IsBusy}" HorizontalAlignment="Center" />
                    <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40, 0 ,0 ,0" Text="{Binding ProgressReport}" FontSize="13"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</ui:UiPage>
