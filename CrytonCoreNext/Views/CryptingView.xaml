<ui:UiPage
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:models="clr-namespace:CrytonCoreNext.Crypting.Models"
    x:Class="CrytonCoreNext.Views.CryptingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="clr-namespace:CrytonCoreNext.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:CryptingViewModel}"
    MinWidth="900"
    d:DesignHeight="850"
    d:DesignWidth="1400"
    mc:Ignorable="d">
    
    <Grid Margin="10, 0 ,0 ,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Crypting" FontSize="32" FontWeight="Light" />
        <Grid Grid.Row="1" Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <ui:Button Icon="Add20" Command="{Binding LoadCryptFilesCommand}" Content="Load files" Padding="6" Margin="0, 10, 10, 10" Appearance="Secondary" HorizontalAlignment="Stretch" />
                <ui:Button Icon="Save20" Content="Save selected" Padding="6" Appearance="Secondary" HorizontalAlignment="Right" />
            </StackPanel>
        </Grid>
        <ScrollViewer Grid.Row="2" Grid.Column="0" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <ListView ItemsSource="{Binding Files}">
                <ListView.Template>
                    <ControlTemplate TargetType="ListBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderBrush}">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </ListView.Template>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="models:CryptFile">
                        <Grid Height="40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="20"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left"
                                        FontSize="13"
                                        FontWeight="Medium"
                                        TextWrapping="WrapWithOverflow"
                                        Text="{Binding NameWithExtension}"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left"
                                        FontSize="12"
                                        Text="{Binding Date}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right"
                                        FontSize="12"
                                        Text="{Binding Size}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right"
                                        FontSize="12"
                                        Text="{Binding Status}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</ui:UiPage>
