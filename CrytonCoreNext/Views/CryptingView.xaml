<ui:UiPage
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Class="CrytonCoreNext.Views.CryptingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="clr-namespace:CrytonCoreNext.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:CryptingViewModel}"
    MinWidth="900"
    d:DesignHeight="850"
    d:DesignWidth="1400"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Crypting" FontSize="48"  />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                <ui:Button Icon="DocumentAdd20" Command="{Binding OpenSnackbarCommand}" Content="Load file" Padding="6" Appearance="Secondary" HorizontalAlignment="Stretch" />
                <ui:Button Icon="Save20" Content="Save file" Padding="6" Appearance="Secondary" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:UiPage>
